# No consumer ProGuard/R8 rules required: the library uses no reflection beyond
# gson on its own model-free JSON parsing. Add rules here if you obfuscate host
# code that implements the Engine interface.
